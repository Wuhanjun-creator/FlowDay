# 节奏洞察页面和导航栏增强计划

## 一、节奏洞察页面柱状统计图增强

### 1. 修改图表生成逻辑

**文件：`src/app.js`**

* 修改 `updateInsights` 函数中的图表生成逻辑

* 将单层柱状图改为三层堆叠柱状图

* 每层分别代表：

  * 上层：核心任务（core）

  * 中层：重要任务（important）

  * 下层：普通任务（normal）

### 2. 增强图表样式

**文件：`styles.css`**

* 添加堆叠柱状图的CSS样式

* 为不同优先级的任务定义不同的颜色

* 保持与现有设计风格一致

### 3. 数据处理逻辑

**文件：`src/app.js`**

* 为每天的任务按优先级分类统计

* 计算每层任务的完成率

* 生成相应的CSS变量用于控制柱状图高度

## 二、左边导航栏增加更多功能

### 1. 添加导航项

2.添加记录今日的导航，在应用设置的下面，用户可以在这里写简单的日志

<br />

## 三、技术实现细节

### 图表实现

* 使用CSS Grid或Flexbox实现堆叠柱状图

* 为每个优先级层级使用不同的颜色

* 保持图表响应式设计

### 导航栏实现

* 保持现有的 `.nav-item` 类结构

* 为新导航项添加相应的 `data-nav` 属性

* 实现平滑的页面切换效果

### 数据处理

* 优化现有的任务过滤逻辑

* 确保图表数据计算准确

* 保持代码的模块化和可维护性

## 四、设计风格保持

* 所有新添加的元素都遵循现有设计系统

* 使用相同的颜色变量和CSS类

* 保持一致的间距、字体和视觉效果

* 确保新功能与现有功能无缝集成

